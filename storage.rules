rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // All files are publicly readable.
    // Writes are only allowed from the backend service account, which bypasses these rules.
    match /{allPaths=**} {
      allow read;
      allow write: if false;
    }
  }
}
